angular.module("kulygin").controller("RadgjafarverkefniController", [
  '$scope',
  '$location',
  '$rootScope',
  '$routeParams',
function($scope, $location, $rootScope, $routeParams) {
  // TODO stuff
}]);
